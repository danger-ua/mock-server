# Git and tooling
.git
.gitignore
.cursor

# Local artifacts
bin
coverage.out

# Test files are not needed in the runtime image
**/*_test.go

Makefile
*.md

# Docker (avoid recursive copy noise)
Dockerfile
docker-compose.yml
.dockerignore
